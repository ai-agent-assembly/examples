{
  "name": "@agent-assembly-examples/approval-gates",
  "version": "0.0.0",
  "private": true,
  "description": "Agent Assembly approval gates scenario — TypeScript implementation",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "//typescript": "Pinned to ^5.9.3: TypeScript 6.0.x regresses NodeNext resolution of node: builtin imports (TS2591 'Cannot find name node:fs'). Bump to ^6 once fixed upstream.",
  "scripts": {
    "build": "tsc --noEmit",
    "start": "node --import tsx/esm src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "js-yaml": "^4.2.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^26.0.0",
    "tsx": "4.22.4",
    "typescript": "^5.9.3",
    "vitest": "^4.1.9"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"],
    "overrides": {
      "esbuild": ">=0.28.1",
      "vite": "^7.3.5"
    }
  }
}
