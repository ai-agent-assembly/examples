{
  "name": "@examples/custom-tool-policy",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal TypeScript example of Agent Assembly tool governance — no agent framework required",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "start": "node --import tsx/esm src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agent-assembly/sdk": "0.0.1-rc.6"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@agent-assembly/sdk", "esbuild", "protobufjs"]
  }
}
